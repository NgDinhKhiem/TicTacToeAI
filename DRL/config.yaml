board_size: 10
win_length: 5
training:
  use_seed: true
  seed: 1337
  total_iterations: 5000
  games_per_batch: 16
  ppo_epochs: 4
  minibatch_size: 64
  learning_rate: 0.0003
  gamma: 0.99
  gae_lambda: 0.95
  clip_range: 0.2
  entropy_coef: 0.01
  value_coef: 0.5
  max_grad_norm: 0.5
  checkpoint_interval: 100
  max_checkpoints: 5
  opponent_pool_size: 3
  eval_random_games: 20
  early_stop_win_rate: 0.9
  device: auto
  max_vram_gb: null
  show_self_play_progress: false
  show_eval_progress: true
checkpoint_dir: checkpoints
log_interval: 10
